# Specify the path of the 'categorization' folder.
path = "C:/Users/aronv/OneDrive/Desktop/git/categorization"

# Use multi-core processing to speed up computations.
# This requires to install the package 'joblib'.
use_multiprocessing = True

# Select the number of cores used for multiprocessing.
# If left as 'None', defaults to using all cores.
num_cores = None